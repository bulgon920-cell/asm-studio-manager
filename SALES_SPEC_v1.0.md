# 月次売上 設計書 v1.0

作成日: 2026-08-02
状態: 確定(方針転換版。旧v1「ASM側でShootsから再計算」は破棄)
位置づけ: 現行の売上管理シート(「スタジオデイズ顧客名簿&月毎売上」)をASMのWeb画面から
参照できるようにする、読み取り専用のビュー機能。

---

## 0. 設計原則

1. **現行シートが唯一の正本。** ASM側では一切再計算しない。月次シート・年次分析シートを
   そのまま読み取って画面に表示するだけ。
2. **読み取り専用。** 現行スプレッドシートには1バイトも書き込まない。
3. **セル位置は実物から特定し、コードに明記する。** 月次シートはTemplate複製でレイアウトが
   固定されているため、読み取るセル範囲は最初に実物を読んで特定し、コードに
   コメント付きのブロック定義として記録する(推測しない)。
4. **エラー値は静かに変換する。** #REF!/#DIV/0!等は「—」として返し、生のエラー文字列を
   画面に出さない。ただしエラーセルがあること自体は件数で示し、隠さない。

## 1. フェーズ

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| 売上タブ(読み取り) | 現行の月次シート・年次分析シートを読んでWebに表示 | 売上タブの数字が現行シートの表示と完全一致する(再計算しないため、一致しない場合は読み取り位置のバグ) |

- 旧v1で想定していた「V2検算」「V3 AI経営コメント」は、この方針転換により前提が変わる。
  必要になった時点で改めて設計する。

## 2. データ源と集計仕様

- 集計元: 現行スプレッドシート(00_config.jsの定数 `SALES_SOURCE_ID`)の
  月次シート「YYYY年MM月」、および年次分析シート。
- 月次シートから読み取る項目: スタジオ月合計(売上・件数・単価)、ジャンル別
  (売上・件数・単価)、目標・前年同月、単月・累計、旬別(1〜10日/11〜20日/21日〜末)。
- 年次シートから読み取る項目: ジャンル×月の件数・売上、合計、前年比、構成比。
- ASM側のShoots等からの再計算は一切行わない。旧方針にあった以下は全て破棄する:
  - 「Shootsから毎回全再計算」する集計エンジン
  - 目標値を手入力する新シート `Sales_Targets`
  - `confirmAdultSelect` への金額訂正組み込み(Shoots.合計金額の更新)
  - 差分同期(05_sync.js)側の拡張

## 3. API(15_sales.js)

| API | 内容 | 書き込み先 |
|---|---|---|
| getSales(年月) | 指定月の月次シートを読み取って返す | なし(読み取り専用) |
| getAnnual(年) | 指定年の年次分析シートを読み取って返す | なし(読み取り専用) |

- セルのエラー値(#REF!等)は「—」に変換し、エラーセルの件数を別途返す。
- 戻り値にDate型を含めない(10_web_api.jsのsanitizeForClient_を通す)。
- 月次シートの実際のセル範囲は 15_sales.js 内のブロック定義コメントを正とする
  (この設計書には、実物確認済みの座標が固まるまで詳細を記載しない)。

## 4. 画面(Webの「売上」タブ)

- 月次ビュー: 当月サマリー/ジャンル別テーブル(旬別は折りたたみ)/前月・翌月ナビゲーション。
- 年次ビュー: ジャンル×12ヶ月マトリクス+前年比較(タブ内切り替え)。
- シートにエラーセルがある場合「元シートに数式エラーがN箇所あります」と小さく表示する
  (隠さず、かといって騒がない)。
- 全て読み取り専用。目標値・金額の編集・訂正は現行シート側で行う(v1では画面から編集しない)。

## 5. 確定事項

1. 計上基準・金額訂正等の実務上の扱いは、すべて現行シート側の運用のまま変更しない
   (ASM側は表示するだけのため、正本側の運用ルールに一切影響を与えない)。
2. 「大本Master_Log引退計画」全体の中でのこの機能の位置づけ(検算・OCR切替判断等)は、
   方針転換に伴い一旦保留。現行シート読み取りが安定してから改めて検討する。
