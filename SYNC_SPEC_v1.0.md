# 差分同期 設計メモ v1.0 (SYNC_SPEC)

作成日: 2026-08-02
目的: 大本Master_Logに増え続ける新規カルテ行を、新環境(ASM)へ安全に追記し続ける。
位置づけ: 移行スクリプト(一度きり)とは別物。追記専用・非破壊の常設機構。

---

## 1. 原則

1. **読み先は「大本」Master_Log(稼働中のもの)。スナップショットではない。** 大本へは1バイトも書き込まない(読み取り専用)。
2. **新環境への操作は「追記」のみ。** 既存のCustomers/Shoots/Ordersの行は絶対に変更・削除しない。特にOrders.status(人がWebで更新した状態)には触れない。
3. **迷ったら止まる。** 想定と違う状態を検出したら、推測で続行せず、エラー内容を報告して停止する(999問題の教訓)。
4. 既存のPDF→OCR→Master_Log→月次売上のツールには一切手を加えない。

## 2. 差分の検出方法

新環境のConfigに同期位置を2つの値で記録する:

| Configキー | 内容 |
|---|---|
| SYNC_LAST_ROW | 前回処理済みの大本Master_Logの最終行番号 |
| SYNC_LAST_HASH | その行の内容ハッシュ(日付+お名前+ジャンル+金額から生成) |

実行時の手順:

1. 大本のSYNC_LAST_ROW行を読み、ハッシュを再計算して SYNC_LAST_HASH と照合する
2. **一致** → SYNC_LAST_ROW+1行目以降が差分。処理へ進む
3. **不一致** → 大本で行の挿入・並べ替え・編集が起きた可能性。**何もせず停止し、状況を報告**(人が確認して同期位置を再設定する)

- OCRツールは末尾追記型である前提。この前提が崩れたときに黙って壊れず止まる、が上記ハッシュ照合の役割。
- 過去行の「編集」は同期対象外(仕様)。過去データの修正が必要な場合は新環境側を直接直す運用とし、頻発するようならその時に設計する。

## 3. 差分行の処理(1行ごと)

移行スクリプトと同じ変換を、**既存データとの突き合わせモード**で行う:

1. **顧客同定**: 新環境の既存Customers/Family_Membersを読み込み、移行と同じキー(①子どもの名前+誕生日 ②顧客名+連絡先 ③顧客名+住所)で照合。
   - 一致 → 既存customerIdを使う(顧客行は変更しない。連絡先等が新しく判明していても自動上書きはしない)
   - 不一致 → 新規Customer追記。既存に同名がいれば要確認フラグ
2. **Shoots追記**: 1行→1撮影。shootId採番は既存の同日連番の続きから。
3. **Family_Members追記**: 未知の子どものみ追加。
4. **Orders起票**: 移行時の「過去案件ルール」ではなく、**新規案件テンプレート**(WEB_SPEC §2.6)を使う:
   - 成人系ジャンル → 「成人商品(セレクト前)」status=店頭セレクト待ち
   - その他の既知ジャンル → 「撮影データ」status=データ納品待ち
   - ジャンル不明 → 注文を作らず Shoots.要確認=TRUE
   ※新規カルテは現在進行中の仕事なので、要確認ではなく最初からアクティブな状態で起票する。
5. **Event_Log追記**: 経路=「同期」で全生成レコードを記録。
6. 全行成功後に SYNC_LAST_ROW / SYNC_LAST_HASH を更新(途中失敗時は更新しない=次回同じ位置から再実行できる)。

## 4. 実行モード

| 関数 | 動作 |
|---|---|
| syncDryRun() | 差分行数・生成予定件数・同定結果をログと「同期レポート」シートに出すだけ。書き込みなし |
| syncRun() | 本実行。LockService必須 |

- 導入手順: syncDryRun で数件の差分を確認 → syncRun 手動実行を数日続ける → 安定したら朝6:30トリガーで sync → morningUpdate の順に自動実行(W4で統合)。

## 5. 実装上の注意

- ファイルは 05_sync.js 1本。Web Dashboard関連ファイルには触れない(並行開発の分離)。
- 大本のスプレッドシートIDは Config ではなくコード内定数 SOURCE_MASTER_LOG_ID(00_configに追加)。
- 移行スクリプトの norm_ / childKey_ / toDate_ 等のユーティリティを再利用する(重複実装しない)。
