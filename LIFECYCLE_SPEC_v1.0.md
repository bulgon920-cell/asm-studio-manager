# Lifecycle 節目一覧 設計書 v1.0

作成日: 2026-08-02
状態: 確定(§6は全て回答済み・本文に反映済み)
位置づけ: P6着手機能「戻す(売上を作る)」の第一歩。まず一覧画面1枚だけを。
提案文の自動生成・DM連携は、この一覧を実際に使ってから判断する。

---

## 0. 設計原則

1. **読み取り中心。** 書き込みは「案内済み」マークの1つだけ。
2. **節目の定義はConfigに置く。** コードではなくシートの行なので、営業側要望の調整(月数・年齢)はそちらで直接できる。
3. **提案メモを主役にする。** 誰にいつではなく、何と言って提案するかまで画面に出す。
4. 除外条件顧客(その他撮影)は対象外。誕生日未登録の子は一覧末尾に「誕生日未登録」として件数のみ表示(データ品質の気づき)。

## 1. 節目マスタ(Config 区分=節目)

それぞれの段階そのまま初期データにもつ列: 節目名/型/条件/表示開始/提案メモ

| 節目名 | 型 | 条件 | 表示開始 | 提案メモ(初期値・編集可) |
|---|---|---|---|---|
| お宮参り | 誕生日型 | 生後0〜3ヶ月 | 誕生直後 | 生後1〜3ヶ月が目安 |
| ハーフバースデー | 誕生日型 | 生後6ヶ月 | 2ヶ月前 | 寝返り・ハイハイができると撮影バリエーションが増える |
| 1歳バースデー | 誕生日型 | 満1歳 | 2ヶ月前 | スマッシュケーキやファーストシューズ提案 |
| 2歳記念 | 誕生日型 | 満2歳 | 2ヶ月前 | 女の子なら次の3歳は七五三へ接続 |
| 七五三(3歳・女) | 年次型 | その年に満3歳(女) | 3月から | 満年齢推奨。数えなら早生まれは満年齢が1つ上・着物サイズ選びに影響 |
| 七五三(5歳・男) | 年次型 | その年に満5歳(男) | 3月から | |
| 七五三(7歳・女) | 年次型 | その年に満7歳(女) | 3月から | ランドセルと同時撮影プラン |
| ランドセル(入学) | 年次型 | 翌年に小学校入学 | 前年10月から | 7歳女は七五三と同時プラン |
| ハーフ成人式 | 誕生日型 | 満10歳 | 3ヶ月前 | |
| 中学入学 | 年次型 | 翌年に中学入学 | 前年12月から | |
| 成人式 | 年次型 | 二十歳の前年から | 前年4月から | 振袖層への提案に特化。振袖在庫4本・自社対応。大量案件は狙わない |
| 背の順家族写真 | 経過型 | 末っ子が満7歳以上かつ最終来店から11ヶ月以上 | ー | 背の順に並んで、毎年一枚の家族写真 |

- 型は3種: **誕生日型**(誕生日からの月齢・年齢で判定)/**年次型**(その年に対象年齢へ達する子をシーズン開始月から表示)/**経過型**(前回撮影からの経過で判定)。
- 経過型(背の順家族写真)はv1に含める。§6-3で判断済み。

## 2. 画面(Web Dashboardに「節目」タブを追加)

- 「今月」〜3ヶ月先までの節目対象を、期日の近い順に一覧表示。
- 1行の表示項目: 子の氏名(サブネーム)/節目名/節目の期日(誕生日・対象月)/保護者名/連絡先/LINE有無/最終来店(日付・ジャンル)/**提案メモ**/[案内済み]ボタン
- 行タップで既存の案件詳細(過去の来店履歴)へジャンプ。
- **済みフィルタ**: 該当ジャンルを今年既に撮影済みの子は自動で除外(例: 今年七五三を撮った3歳児は出さない)。
- 「案内済み」を押すと一覧からグレーアウト(非表示ではなく薄く残す)。

## 3. データと書き込み

- 判定材料: Family_Members(誕生日・性別)+Shoots(撮影履歴)+Customers(連絡先・LINE有無)。全て既存の正本のみ。新しい正本は作らない。
- 「案内済み」記録: 新シート Lifecycle_Contacts(memberId/節目名/対象年/案内日/操作者)へ追記専用。Event_Logにも経路=Web(節目)で記録。
- 翌年は同じ節目でも対象年が変わるので別表示させる(案内済みは節目名+対象年の組で判定)。

## 4. AIの役割

- v1では**なし**。判定は全て通常ロジック(年齢計算とジャンル照合)。
- 提案文の自動生成・DM連携は、一覧を1シーズン使ってから判断(旧Lifecycle Engineの轍を踏まない)。

## 5. やらないこと(v1)

- DM・LINE配信/提案文生成/印刷宛名などの自動化/学校・企業案件の対象外/誕生日未登録の子の推定年齢表示

## 6. 確定事項(旧: 未確定事項)

1. **七五三は満年齢基準。** 数え年の子は一覧に出さない(基本として満年齢を推奨するため)。数え希望の「お家庭」への対応は接客時のヒアリングで行う。提案メモにはその旨、「数えなら早生まれは満年齢が1つ上」と記載。
2. **成人式は二十歳の前年4月から表示。** 競合(写真館系スタジオ)と競っても勝ち目が薄いため、振袖層への提案に特化。振袖在庫4本・自社対応のみ。大量案件は狙わない前提。
3. **背の順家族写真はv1に含める。** 条件: 末っ子が満7歳以上の家族で、最終来店(ジャンル不問)から11ヶ月以上経過。節目というより「毎年つながっているかを確認するリマインド」の中核施策。
