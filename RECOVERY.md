# 復旧手順書(新環境スプレッドシートが壊れた・消えたとき)

作成日: 2026-08-02
所要時間の目安: 15〜20分

## この手順書について

「AI_Studio_Manager_本体」のスプレッドシートを誤って削除してしまった、内容が
おかしくなってしまった、というときに、直近の自動バックアップから復旧するための
手順です。IT専門知識がなくても、上から順番に進めれば復旧できます。

## 手順

### ステップ1: バックアップを探す

1. Googleドライブを開く
2. 「ASM_backup」フォルダを開く
3. 「ASM_backup_日時」という名前のファイルが並んでいます。**一番新しい日付・
   時刻のもの**(通常は一番上)を選ぶ

### ステップ2: バックアップをコピーして本番用にする

1. 見つけたバックアップファイルを右クリック →「コピーを作成」
2. コピーされたファイルの名前を、わかりやすい名前に変更する
   (例:「AI_Studio_Manager_本体_復旧_20260802」)。
   **これ以降、この新しいファイルが「新環境スプレッドシート」本体になります。**
3. このファイルを開いた状態でブラウザのアドレスバーを見る。
   `https://docs.google.com/spreadsheets/d/【この部分】/edit` の
   【この部分】の文字列(スプレッドシートID)をメモ帳などに控えておく

### ステップ3: 接続先を切り替える

1. リポジトリの `00_config.js` を開く
2. `TARGET_SPREADSHEET_ID` の値を、ステップ2で控えたIDに書き換える
3. 保存してGitHubへcommit・push
4. `clasp push` でGoogle Apps Scriptプロジェクトへ反映する

*(自分で作業するのが不安な場合は、Claude Codeに「新環境スプレッドシートが
壊れたので、バックアップのスプレッドシートID ○○○○ に切り替えてください」と
伝えれば、この作業を代行してもらえます)*

### ステップ4: トリガーを再設置する

1. Google Apps Scriptのスクリプトエディタを開く
2. 上部の関数選択リストから `installMorningTrigger` を選んで実行
   (朝の自動更新トリガー)
3. 同様に `installBackupTrigger` を選んで実行(日次バックアップトリガー)
4. どちらも実行ログに「トリガーを設置しました」と出れば成功

### ステップ5: Web画面で表示確認する

1. いつものWebアプリのURLを開く(すでに開いていた場合はブラウザを
   ハードリロード: Windowsは `Ctrl+Shift+R`)
2. 「今日の予定」「要確認」「やること」が正しく表示されることを確認する
3. 問題なく表示されれば復旧完了です

## 禁止事項

- **バックアップのファイルを直接編集しない。** 必ず「コピーを作成」した
  新しいファイルの方を使う。元のバックアップは、万一この復旧作業自体が
  失敗したときのために、そのまま手を付けずに残しておく
- 複数のバックアップコピーを同時に「本番用」として使わない(どれが正か
  分からなくなる)
- 壊れる前の元のスプレッドシート(まだ存在する場合)をこの時点で削除しない。
  原因調査のためにそのまま残しておく

## 困ったときは

この手順で解決しない場合、または何が起きたのか分からない場合は、無理に
自己判断で進めず、見えている状況(エラーメッセージ・画面の様子など)を
そのまま記録してから、開発担当(Claude Code経由)に相談してください。
