# ASM Studio Manager 開発ルール

## プロジェクト概要
写真スタジオ業務OS。設計書は 設計書_v1.0.md を正とする。
Web実装は WEB_SPEC_v1.0.md を正とする。
設計書にない構造変更(シート・列・statusの追加変更)は、実装前に必ず提案して承認を得ること。

## ワークフロー(毎回この順で)
1. 変更内容を先に説明し、承認を得てから編集する
2. ローカルで編集(このリポジトリが正。Apps Scriptエディタ側は編集しない)
3. `node --check` で全 .js の構文検査
4. git commit(日本語で「何を・なぜ」を書く)
5. `clasp push`(--force は使わない)
6. push結果とエディタでの確認手順を報告する
7. **WebApp.htmlまたはWeb API(10_web_api.js/11_web_write_api.js/12_web_review_api.js/
   13_morning.js/14_lifecycle.js等)に変更を加えた場合、完了報告に「新バージョンの
   デプロイが必要」と必ず明記する。** clasp pushはコードを反映するだけでデプロイ済み
   Webアプリには自動反映されないため、これを案内し忘れると古いバージョンのまま
   検証されてしまう(2026-08-02に実際に発生)。

## 禁止事項
- 中身のある既存GASプロジェクトへの `clasp push --force`
- appsscript.json の timeZone を Asia/Tokyo 以外にすること
- 承認なしのファイル削除・リネーム
- 実顧客データをコード・コメント・テストに書くこと(テストはダミーデータのみ)
- `clasp create` の再実行(接続先は `.clasp.json` が正。迷ったら停止して報告)
- `setupNewEnvironment()` の再実行提案(新環境はすでに存在する)

## clasp push が差分エラーで失敗したとき
--force で解決しない。原因を調べて報告し、指示を待つこと。

- appsscript.jsonにwebapp設定を入れて以降の実際の挙動(2026-08-02に検証済み):
  - ローカルとリモートに**本当に差分が何もない**ときは `clasp push` は
    `Script is already up to date.` で警告なく完了する。
  - **他のファイルに実際の変更があってpushが発生するとき**、appsscript.jsonの
    中身自体に差分がなくても、webapp設定を含むためclaspが確認を求め
    「Skipping push.」で止まることがある(clasp側の仕様と推測)。
  - 回避専用のclaspフラグは存在しない。この場合は、別ディレクトリへの
    `clasp pull` でリモートの実際の内容を取得し、ローカルとの差分(特に
    appsscript.json)を確認したうえで、意図した変更のみであることを報告してから
    `--force` を使ってよい(2026-08-02にユーザー承認)。「00_config.jsのID設定ズレ」
    のような恒常的な原因ではなく、変更をpushするたびに同じ手順が必要になる想定。

## 実行について
関数の実行・OAuth承認・実行ログの確認はユーザーがブラウザの
スクリプトエディタで行う。Claude Codeは実行を代行できない前提で、
「どの関数を実行し、ログの何を確認すべきか」を毎回具体的に伝えること。
