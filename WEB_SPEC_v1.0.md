# AI Studio Manager Web Dashboard 設計書 v1.0

作成日: 2026-08-02
状態: 確定(この設計書が正。逸脱する実装は事前承認が必要)
前提: SPEC_v1.0.md(データスキーマ)の新環境が本移行済みであること

---

## 0. 設計原則

1. **Webが唯一の書き込み入口。** シートは閲覧・非常時のみ。全ての変更はWeb API経由でEvent_Logに残る。
2. **朝はこの1画面だけ開けば足りる。** 完成判定は「14日連続、スプレッドシートを1枚も開かずに業務が回ること」。
3. **状態遷移はサーバー側で検証する。** 画面には「その状態から進める先」のボタンしか出さず、APIも不正な遷移を拒否する。
4. **1画面=1ファイルの単純構成。** SPA框架は使わない。GAS HtmlService+素のHTML/JS/CSSで、スマホ優先レイアウト。

---

## 1. 画面構成(3画面のみ)

### 1.1 今日の画面(ホーム)

朝開く画面。上から順に:

```
┌─────────────────────────────┐
│ [朝の更新] 最終更新: 8/2 07:30      │
├─────────────────────────────┤
│ ■ 今日の撮影 (2件)                │
│  10:00 七五三 ○○様 → 詳細       │
│  14:00 お宮参り ○○様 → 詳細      │
├─────────────────────────────┤
│ ■ 要確認 (17件) ← 移行直後は多い    │
│  [注文] ○○様 実状態を確定する      │
│  [顧客] 同名顧客の確認             │
├─────────────────────────────┤
│ ■ やること (アクティブな注文)        │
│  データ納品 ○○様 期限8/5 [完了]    │
│  発注 ○○様(雅) [仕上がり待ちへ]    │
├─────────────────────────────┤
│ ▼ 待ち (折りたたみ・既定で閉じる)     │
│  仕上がり待ち ○○様 予定8/10        │
└─────────────────────────────┘
```

- 並び順: 期限昇順 → 期限なしは撮影日昇順。
- 各行タップで案件詳細へ。主要ボタン(完了・次へ)は一覧上にも置き、1タップで進められる。
- 「待ち」は既定で閉じる(見なくていいものを見せない)。仕上がり予定日を過ぎた「仕上がり待ち」は自動で「やること」へ浮上する。
- 「要確認」「やること」は系統で2ブロックに分割する: 上=プリント・商品(外注、先手優先)、
  下=データ(セレクト)。系統はConfigの注文種別マスタ(§2.6)から引く(商品系・作業系は
  上ブロック、データ系は下ブロック)。撮影・顧客単位の要確認(系統を持たない)は
  ブロックの外に別枠で表示する。
- 各カードの右上に、撮影日からの経過日数バッジ「N日目」を表示する。ブロック内の並び順は
  経過日数の降順(古いものほど上)。バッジの色: 〜4日=通常 / 5〜7日=琥珀 / 8日以上=赤
  (データ納品の約束は撮影日から1週間のため)。
- 基本フォントサイズは16px以上。案件詳細画面はさらに大きくする。背景色に対して
  文字色のコントラストを確保する(薄いグレーの多用を避ける)。

### 1.2 案件詳細

```
○○様 (C-00038) 七五三 2026/08/02
├ 家族: はな(7才) たろう(3才)
├ 過去の来店: 2024/11 七五三 → 詳細
├ Drive: [元データ] [オンラインアルバム]
├ 注文一覧:
│  O-00301 撮影データ [データ納品待ち]
│    → [完了にする] [保留] [不要]
│  [+ 注文を追加]
└ メモ: ____________ [保存]
```

- 成人商品(セレクト前)の注文には「セレクト確定」ボタン: 白台紙/雅/ローズを選び、オプション(美肌補正・全データ)にチェックすると、種別変更+オプション注文行の追加+status=発注待ちへの遷移を1操作で行う。
- 要確認注文には「実状態を確定」: 現在の実際の状態(完了/納品連絡待ち等)を選ぶだけ。

### 1.3 完了・履歴

- 完了(移行時推定を含む)・不要になった注文の一覧。月別。
- 検索: 顧客名・子ども名で過去案件を探す(電話対応時用)。

---

## 2. 状態遷移の実装

- Configのstatus一覧に「遷移可能先」を持たせる(例: 発注待ち → 仕上がり待ち/保留/不要)。
- 画面はこの表からボタンを生成。APIは遷移表にない変更を全て拒否し、拒否もEvent_Logに残す。
- 全ての変更は 変更前→変更後 をEvent_Logへ追記してからOrdersを更新する。

## 3. API一覧(GAS側関数)

| API | 内容 | 書き込み先 |
|---|---|---|
| getToday() | 今日の画面のデータ一式 | なし(読み) |
| getShootDetail(shootId) | 案件詳細+顧客+履歴 | なし(読み) |
| updateStatus(orderId, to) | 状態遷移(検証つき) | Orders+Event_Log |
| updateOrderFields(orderId, {担当,メモ,期限,セレクト予定日}) | 属性更新 | Orders+Event_Log |
| confirmAdultSelect(orderId, 商品, オプション[]) | セレクト確定の複合操作 | Orders+Event_Log |
| addOrder(shootId, 種別) | 注文追加 | Orders+Event_Log |
| resolveReview(対象Id, 内容) | 要確認の解消 | 各正本+Event_Log |
| morningUpdate() | 朝の更新(§4) | Today_Shoots/Matches/Task_Board |

- 顧客統合(同名顧客のマージ)はv1に**含めない**。要確認顧客は「確認済み(別家族)」のマークのみ。統合は運用が回ってから設計する。

## 4. 朝の更新(morningUpdate)

1. Google Calendarから当日(+翌日)の予定を読む → Today_Shoots再生成
2. 顧客・過去履歴と照合 → Today_Shoot_Matches再生成(候補提示のみ。確定は人)
3. Today_Task_Board再生成(Ordersのstatusから導出。§1.1の並び順)

- 全再生成・冪等。失敗時は前回の表示を残し、エラーを画面上部に表示。
- 時間トリガーで毎朝自動実行+画面の[朝の更新]ボタンでも実行可。

## 5. アクセスと権限

- v1: executeAs=USER_DEPLOYING / access=MYSELF(オーナーのみ)。
- 配偶者運用の開始時: access=DOMAIN(Workspace内限定)へ変更し、操作者をEvent_Logに記録。ANYONEは永久に使わない。

## 6. 開発フェーズ(各フェーズに完了条件)

| フェーズ | 内容 | 完了条件 |
|---|---|---|
| W1 読む | 今日の画面+案件詳細(閲覧のみ) | 実データで朝の状況が正しく見える |
| W2 進める | updateStatus/updateOrderFields+遷移検証 | 状態変更がEvent_Logに残り、シート直編集なしで1日回る |
| W3 確定する | 要確認解消+セレクト確定+注文追加 | 移行時の要確認17件を画面から全て解消できる |
| W4 朝が来る | morningUpdate+Calendar連携+トリガー | 朝、何も操作せず画面に今日が出ている |
| 完成判定 | — | 14日連続シートゼロ運用 |

- W1完了時点から毎朝実際に使い始める(並走検証)。完璧を待たない。

## 7. やらないこと(v1)

- 顧客統合UI / Lifecycle・DM機能 / OCR / 通知(LINE・メール) / 印刷用レイアウト / 複数店舗対応 / ダークモード等の装飾

## 8. 確定事項(旧: 未確定事項)

1. **カレンダーID**: `bulgon920@yahoo.co.jp`(メインカレンダー)。コードでは定数 CALENDAR_ID として00_config相当のファイルに置く。
2. **予定の書式(実物確認済み)**:
   - 約70%はオンライン予約システムが自動登録。タイトル `●ジャンル プラン for 姓`、説明欄にラベル付きで Client's phone number / Client's email / ご来店人数 / 主役のお子様の年齢・性別 と自由メモ(リピ様、LINEやりとり有 等)。
   - 残り約30%は手動登録で書式が揺れる。**読めた項目だけ構造化し、読めない予定もタイトルのまま一覧に必ず表示する(黙って落とさない)。**
   - 照合キー: 姓+電話番号(説明欄から抽出、+81→0へ正規化)。
3. **朝の自動更新**: 6:30(時間トリガー)。変更は定数1箇所。

## 9. 実装上の追加要件

- **LockService必須**: 全ての書き込みAPIは LockService.getScriptLock() で直列化する(PCとスマホの同時操作対策)。
- **appsscript.json**: timeZone=Asia/Tokyo / webapp: executeAs=USER_DEPLOYING, access=MYSELF。
- 外部ライブラリ・CDN・フレームワークは使わない。素のHTML/CSS/JSのみ(非エンジニア保守と依存ゼロを優先)。
- Web Appの初回デプロイと権限承認はユーザーがブラウザで行う。
